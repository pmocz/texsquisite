% I am a LaTeX file. I have errors.

hello world              % trailing whitespace
hello ''world''          % quote begins with '
hello 'world'            % quote begins with '
``hello`` world          % quote ends with `
`hello` world            % quote ends with `
``hello" world           % quote ends with "
"hello" world            % quote uses "
hello~\ref{world}        % missing `~'
hello\footnote{world}   % whitespace before footnote
hello~ world             % double space
\[math\].                % punctuation mark after end of display math
\[math\],                % punctuation mark after end of display math
$math.$                  % punctuation mark before end of math mode]
$math,$                  % punctuation mark before end of math mode

